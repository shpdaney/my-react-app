import React from "react";
import styles from './index.module.scss'

export const Comp: React.FC = (/*{Props}*/) => {
    return (
        <section className={styles.comp}>
            <h2 className={styles.compTitle}>COMP</h2>
        </section>
    )
}